Workflows are SOPs that describe objectives, inputs, tools, and outputs.

Place workflow markdown files in this directory. Each workflow should include:
- Objective
- Inputs (required files/credentials)
- Tools to use (scripts in `tools/`)
- Outputs and where to store them

Do not overwrite existing workflows without confirmation.
